# 🏗 从零实现一个 Go + Web3 钱包系统
---

# 🚀 明确项目最终目标（产品形态）

最终你将实现一个完整可展示的 Web3 项目：

---

# 🎯 **项目最终功能**

## 🔐 1. **SIWE（Sign-in With Ethereum）登录**

* 前端让用户用 MetaMask 签名一次
* Go 后端验证签名 → 生成 JWT → 用户登录成功

## 💰 2. **查看用户 Token 资产**

* 支持 ERC20
* 支持原生 ETH
* 支持多 Token（后端查询链上/Alchemy，缓存）

## 🧾 3. **查看用户历史交易**

* 查询普通转账
* 查询 ERC20 Token 转账
* 分页返回

## 💸 4. **发送 Token / ETH**

* 用户在前端发起转账 → 后端构造交易 → 前端用钱包签名 → 后端广播交易

## 🖼 5. **简单 NFT Mint（演示工程能力）**

* 提供一个智能合约（Solidity）
* 后端提供 mint API
* 前端点击即可 mint

---

# 🎬 最终展示效果（GitHub README 要有）

* 架构图
* 前端界面截图
* NFT mint 演示
* 用户账户面板
* 交易列表
* 后端 swagger 文档
* 合约部署地址（Goerli / Sepolia）

你简历就可以写成：

> 独立设计并实现一个 Go + Web3 DApp 系统，覆盖钱包登录、链上事件监听、Token 资产管理、交易发送等核心功能，并配套 Solidity 合约与全栈架构。

---

# 🧱 第 1 步：架构设计

下面是我们要实现的整个系统结构（你能在面试时讲清楚）：

```
User (MetaMask)
      │
      ▼
Frontend (Vue3 + ethers.js)
      │
      ├── Sign message
      │
      ▼
Backend (Go + Gin)
      ├── Verify signature (SIWE)
      ├── JWT login
      ├── Get balances (ETH/ERC20)
      ├── Get transaction history
      ├── Send transaction (raw tx)
      ├── NFT mint (call contract)
      └── Event listener (optional)
      │
      ▼
Ethereum Sepolia (Testnet)
      ├── ERC20 Token
      └── NFT Contract
```

这是一个“微型 Web3 钱包系统”，但工程能力很强。

---

# 🛠 第 2 步：技术选型（全部稳定、主流）

### 后端（Go）

| 技术                         | 用途             |
| -------------------------- | -------------- |
| **Gin**                    | Web 框架         |
| **go-ethereum (geth)**     | 链上交互           |
| **gorm + sqlite/postgres** | 存用户、nonce、登录信息 |
| **jwt-go**                 | Token          |
| **viper**                  | 配置             |
| **swagger**                | 文档             |

### 智能合约（Solidity）

| 技术                   | 用途        |
| -------------------- | --------- |
| **Foundry**          | 开发合约      |
| ERC721（OpenZeppelin） | NFT 模板    |
| ERC20（OpenZeppelin）  | Token（可选） |

### 前端（Vue3）

| 技术           | 用途   |
| ------------ | ---- |
| Vue 3 + Vite | 框架   |
| ethers.js    | 钱包交互 |
| pinia        | 状态管理 |
| tailwind     | UI   |

---

# 📁 第 3 步：项目仓库结构（现在我们先创建这个）

我们会采用 monorepo 或多仓库都可以。这里我推荐 **multi repo**（面试更清晰）。

你在 GitHub 创建三个仓库：

### 1. go-web3-wallet-backend

```
/cmd
/internal
    /auth
    /wallet
    /token
    /tx
/contracts    (生成的 abigen 文件)
```

### 2. web3-wallet-frontend

```
/src
/components
/pages
/stores
```

### 3. web3-wallet-contracts

```
/src
/scripts
/test
```


